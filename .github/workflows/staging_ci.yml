name: Create Merge Request

on:
  push:
    branches:
      - '*' 
      - '!main'
      - '!staging'

jobs:
  create-pr:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ssh-key: ${{ secrets.SSH_DEPLOY_PRIVATE_KEY }}

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          branch: staging
          title: ${{ github.ref }} Pull Request
          body: Automatically generated PR from branch ${{ github.ref }}
          draft: true